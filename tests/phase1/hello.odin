//gd:extends Node2D
package phase1_sample

// Phase 1 sample `.odin` script. The marker comment above is the Phase-1
// base-type convention parsed by OdinScript._get_instance_base_type.
// No execution happens yet (Phase 2); this file only needs to load as an
// OdinScript resource and report its source + base type. It registers no
// compiled class, so the engine gives it a placeholder instance.
